import React from "react";
import styles from "./PeriodConsumptionChart.module.scss";

export default function PeriodConsumptionChart({ data, valueKey }) {
  return (
    <div className={styles.chart}>
      {/* TODO: Bar chart by period (day/week/month) using data and valueKey */}
      <p>Graphique de consommation par période – en construction</p>
    </div>
  );
}
