#---------------------------------------------------------
# Adafruit / Hardware stack (Raspberry Pi uniquement)
#---------------------------------------------------------
Adafruit-Blinka==8.66.0 ; sys_platform == 'linux'                     # Compatibility layer for CircuitPython libraries on Raspberry Pi / Linux boards
adafruit-circuitpython-busdevice==5.2.13 ; sys_platform == 'linux'    # I2C/SPI bus device helper classes for Adafruit drivers
adafruit-circuitpython-connectionmanager==3.1.5 ; sys_platform == 'linux'   # Connection/session manager for CircuitPython network libs
adafruit-circuitpython-mcp230xx==2.5.18 ; sys_platform == 'linux'     # Driver for MCP23008/MCP23017 I2C GPIO expanders
adafruit-circuitpython-requests==4.1.13 ; sys_platform == 'linux'     # Lightweight HTTP requests library (API compatible with 'requests')
adafruit-circuitpython-typing==1.12.2 ; sys_platform == 'linux'       # Typing stubs for Adafruit CircuitPython projects
Adafruit-PlatformDetect==3.83.1 ; sys_platform == 'linux'             # Detects board/platform type (Raspberry Pi, BeagleBone, etc.)
Adafruit-PureIO==1.1.11 ; sys_platform == 'linux'                     # Low-level pure Python IO library for I2C/SPI

#---------------------------------------------------------
# Python-to-hardware bridges (Raspberry Pi uniquement, sauf mention contraire)
#---------------------------------------------------------
pyftdi==0.57.1 ; sys_platform == 'linux'   # USB-to-serial/I2C/SPI via FTDI adapters (dépendance optionnelle de Blinka, non utilisée directement)
pyusb==1.2.1 ; sys_platform == 'linux'     # Direct USB device access (dépendance optionnelle de Blinka, non utilisée directement)
pyserial==3.5                              # Serial port communication (multiplateforme, utilisé aussi hors Pi)
bleak==0.22.3                               # Bluetooth Low Energy (BLE) client, used for BTHome sensor scanning (multiplateforme, gère Windows/Linux/macOS nativement)

#---------------------------------------------------------
# Web backend (Django + DRF)
#---------------------------------------------------------
Django==5.2.1                                           # Main web framework
djangorestframework==3.16.0                             # REST API framework for Django
djangorestframework-simplejwt==5.3.1                    # JWT authentication for DRF (access/refresh tokens)
sqlparse==0.5.3                                         # SQL parser (used by Django migrations/queries)
asgiref==3.8.1                                          # ASGI server reference implementation (required by Django)
tzdata==2025.2                                          # Timezone database (for Windows / environments without system tzdata)

#---------------------------------------------------------
# Deployment
#---------------------------------------------------------
gunicorn==23.0.0             # WSGI/ASGI HTTP server for production
systemd-python==235; sys_platform == 'linux'   # systemd integration (only on Linux)

#---------------------------------------------------------
# Caching / messaging
#---------------------------------------------------------
redis==6.1.0                     # Redis client (caching, pub/sub, task queue, etc.)

#---------------------------------------------------------
# Testing / Dev utilities
#---------------------------------------------------------
pytest==8.3.5                         # Testing framework
pytest-django==4.11.1                 # Django plugin for pytest
pytest-mock==3.14.0                   # Mocking helper for pytest
freezegun==1.5.1                      # Freeze time in tests (simulate specific datetime)
coverage==7.15.2                      # Code coverage measurement
pytest-cov==7.1.0                     # pytest --cov integration (wraps coverage)
iniconfig==2.1.0                      # Config parser used by pytest
pluggy==1.5.0                         # Plugin management (pytest internals)
colorama==0.4.6                       # Colored terminal output (mainly for Windows test/dev UX)
factory_boy==3.3.3                    # Factories for Django/ORM objects in tests

#---------------------------------------------------------
# Misc utilities
#---------------------------------------------------------
python-dateutil==2.9.0.post0   # Extended datetime parsing/handling
python-dotenv==1.1.0           # Load environment variables from .env files
packaging==25.0                # Version parsing & dependency utilities
six==1.17.0                    # Python 2/3 compatibility shim (still required by some deps)
typing_extensions==4.15.0      # Backport of newer typing features

#---------------------------------------------------------
# AI / LLM
#---------------------------------------------------------
groq==1.2.0
